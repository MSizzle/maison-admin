[phases.setup]
nixPkgs = ["nodejs_18"]

[phases.install]
cmds = ["npm install", "cd client && npm install"]

[phases.build]
cmds = ["echo 'Building client...' && cd client && npm run build"]

[start]
cmd = "node server/index.js"
